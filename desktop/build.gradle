apply plugin: 'java'
apply plugin: 'maven'

group = "org.kgbt.passha"
version = '3.0'

description = """"""

repositories {
    maven { url "http://repo.maven.apache.org/maven2" }
}

dependencies {
    compile project(':core')
}
